# Sound Assets

`rain.wav` and `thunder.wav` are generated audio assets created for this project
with Python's standard `wave` module. They are not copied from third-party
sound libraries.

They are distributed under the same MIT license as the rest of the project.
